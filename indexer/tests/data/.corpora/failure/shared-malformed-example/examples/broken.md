This example file opens with prose rather than with the `# [<ID>] <Title>`
heading every example file must carry, which is the one thing wrong with this
corpus.

Statements: `[FIX-008]`

It is cited by both documents beside it. Adding the missing heading repairs it
for both of them at once, so a run reporting the missing heading twice sends its
reader looking for a second defect that does not exist.
