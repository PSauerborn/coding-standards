This example file opens with prose rather than with the `# [<ID>] <Title>`
heading every example file must carry, which is the one thing wrong with this
corpus.

Statements: `[FIX-003]`

It is cited by the document beside it, so a run that also reported it as
uncited would send its author to add a citation that is already there.
