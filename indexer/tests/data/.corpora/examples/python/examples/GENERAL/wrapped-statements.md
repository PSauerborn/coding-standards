# [PY-100] Fixture With A Wrapped Statements Line

Statements: `[PY-100]`
  `[PY-101]`
  `[PY-DOCKER-001]`

The statements line above is wrapped over three lines. Markdown renders the
three as a single paragraph, so every identifier on them is declared by this
example file and none of them may be dropped.
