# [PY-101] Fixture Cleaned Path Target

Statements: `[PY-101]`

This file is cited through `examples/../examples/config.md`, which cleans to a
path inside the source root and must therefore resolve successfully.
